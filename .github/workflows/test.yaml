name: Integration

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  unit-tests:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x, 20.x, 22.x]

    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    - name: "Installing dependencies"
      run: npm ci
     
    - name: Create .env file
      run: |
        echo MONGO_TEST_URL = "mongodb+srv://jaiswalshivanivhits:shivani22@cluster0.djusjqb.mongodb.net/Task_test?retryWrites=true&w=majority&appName=Cluster0" > .env
        echo "JWT_SECRET_KEY = "fjsdfkjslkfsf"" >> .env  # Add more if needed

    - name: "Running Test cases"
      run: npm test -- --detectOpenHandles --verbose

